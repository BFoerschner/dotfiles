#!/bin/bash
set -euo pipefail

# shellcheck source=secrets-platform
source secrets-platform

# Remove symlinks
rm -f "$HOME/.supermaven/config.json" 2>/dev/null || true

# Destroy RAM-backed secrets directory
_secrets_destroy_ramdir

echo "Secrets locked."
