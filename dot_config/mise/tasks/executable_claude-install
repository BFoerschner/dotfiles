#!/usr/bin/env bash
#MISE description="Install/update Claude Code native binary"
install_script="$(mktemp)"
curl -fsSL https://claude.ai/install.sh -o "$install_script"
bash "$install_script"
rm -f "$install_script"
