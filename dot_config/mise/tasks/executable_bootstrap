#!/usr/bin/env bash
#MISE description="Bootstrap full environment: install tools, plugins, and binaries"
#MISE depends=["claude-install", "tmux-plugins-install", "neovim-setup"]
set -euo pipefail

echo "Bootstrap complete!"
